# Basic-Python-Calculator
This is a simple python calculator project using input and if concepts.
# Basic Python Calculator

This is a beginner-friendly calculator written in Python.

## How to run
Make sure Python is installed, then run:
python main.py

Features:
Addition
Subtraction
Multiplication
Division
Division-by-zero protection

### Option B: Upload it from your computer with Git

If you already have the file on your laptop, you can also push it with Git. GitHub’s docs support adding locally hosted code to GitHub from the command line. :contentReference[oaicite:3]{index=3}

Example commands:
git init
git add .
git commit -m "Add basic calculator"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/basic-python-calculator.git
git push -u origin main

Replace YOUR_USERNAME with your GitHub username.
